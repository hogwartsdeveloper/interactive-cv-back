version: '3.9'

services:
  db:
    image: postgres:15
    container_name: postgres
    ports:
      - '5432'
    volumes:
      - './postgres:/var/lib/postgresql/data/'
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB}
    restart: always

  app:
    build: .
    ports:
      - "8000"
    depends_on:
      - db
    command: >
      bash -c '
        npx prisma db push
        npx prisma generate
        npm run build
        npm start
      '

  pgadmin:
    container_name: pgadmin
    depends_on:
      - db
    image: dpage/pgadmin4
    expose:
      - '80'
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
    restart: always

  nginx:
    container_name: proxy_nginx
    depends_on:
      - app
      - db
      - pgadmin
    image: nginx:latest
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - www:/var/www
      - ./nginx:/etc/nginx/conf.d
      - etc-letsencrypt:/etc/letsencrypt
    restart: always

#  certbot:
#    image: certbot/certbot
#    depends_on:
#      - nginx
#    container_name: certbot
#    volumes:
#      - etc-letsencrypt:/etc/letsencrypt
#      - www:/var/www
#    command: certonly --webroot --webroot-path=/var/www --email jannuraidynuly@gmail.com --agree-tos --no-eff-email --staging -d back.sumurai.kz

volumes:
  www:
  etc-letsencrypt: